---
# tasks file for build
- name: add docker file
  copy:
    src: Dockerfile
    dest: /tmp

- name: install packages
  apt:
    pkg:
      - docker.io
      - python3-pip
    update_cache: yes
    state: present

- name: pip install docker
  pip:
    name: docker

